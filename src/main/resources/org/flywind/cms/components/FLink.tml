<t:container xmlns:t="http://tapestry.apache.org/schema/tapestry_5_4.xsd"
 xmlns:p="tapestry:parameter">
 	<t:if test="cur">
 		<a href="#;" class="${activeCls}" t:type="pagelink" t:page="${page}">${value}</a>
 		<p:else>
 			<a href="#;" t:type="pagelink" t:page="${page}">${value}</a>
 		</p:else>
 	</t:if>
</t:container>